# Tree model for Crypto
场景：根据当前bar和历史bar的量价数据，预测下一个bar的收益率。
## 主体框架

* 数据读取模块：载入15m频率的量价数据。
* 特征构建：构建数据对，需要做筛选，根据当前bar变化绝对值超过阈值，比如1%，以当前bar和历史的量价数据构建特征集合X，以下一个bar的收益率为模型输出y。特征构建考虑alpha101里的动量、波动率相关的指标，以及twap，vwap等等，特征集合越多越好，我们可以后续考虑filter out 不相关的特征。
* 模型模块：根据构建好的数据对，我会传入一个tree模型，进行回归，比如Random Forest，Xgboost 等等。这一部分，需要拆分数据集，保留时间序列最后的10%样本作为样本外数据。你需要写一个文档，记录该模型在训练集，验证集，样本外的表现。
* 回测模块：
回测模块接收一个训练好的模型，分别在训练集，验证集，样本外回测模型的表现。交易逻辑：当前bar变化绝对值超过阈值，启动模型，预测下一个bar的收益，如果预测值超过上涨阈值或者下跌阈值，入场交易，持有一个bar出场。
